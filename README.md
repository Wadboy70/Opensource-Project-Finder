# Opensource-Project-Finder


## Get started 


use `yarn dev` to run the front and backend


use `yarn client` or `yarn server` to run their respective folders


yarn dev runs server as a proxy so just use `fetch('/whatever)`


:)